
Kendi adimizla Branch olusturma

git branch <kendi adin'iz>
git checkout <kendi adin'iz>

Sonraki islemler

Pull islemleri once yapilir

git checkout main
git pull
git checkout <kendi adin'iz>

Github ilk push

git add .
git commit -m "Initial commit"
git push --set-upstream origin <kendi adiniz>

Git Push islemi. kendi branchinizda oldugunuzdan emin olun

git add dosya adi
git commit -m "version adi"
git push




